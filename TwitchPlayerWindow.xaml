<Window x:Class="ARC_Sight.TwitchPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Twitch Player" Height="500" Width="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Border Background="#111" BorderBrush="#ff5500" BorderThickness="2" CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Background="#222" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <TextBlock x:Name="StreamerTitleText" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="LockBtn" Content="🔓" Click="Lock_Click" Width="30" Background="Transparent" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="14"/>
                    <Button Content="✕" Click="Close_Click" Width="30" Background="Transparent" Foreground="#ff5500" BorderThickness="0" Cursor="Hand" FontWeight="Bold"/>
                </StackPanel>
            </Grid>

            <wv2:WebView2 x:Name="Browser" Grid.Row="1"/>
        </Grid>
    </Border>
</Window>