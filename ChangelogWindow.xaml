<Window x:Class="ARC_Sight.ChangelogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Patch Notes" Height="500" Width="450"
        WindowStyle="None" ResizeMode="NoResize"
        Background="#1E1E1E"
        WindowStartupLocation="CenterOwner"
        BorderBrush="#ff5500" BorderThickness="1">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleBlock" Text="PATCH NOTES" Foreground="#ff5500" FontWeight="Bold" FontSize="18" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="#252525" BorderBrush="#333" BorderThickness="1" CornerRadius="3">
            <FlowDocumentScrollViewer x:Name="DocViewer" VerticalScrollBarVisibility="Auto" Margin="5">
                <FlowDocument FontFamily="Segoe UI" PagePadding="10" Foreground="#ddd" TextAlignment="Left" IsOptimalParagraphEnabled="True" IsHyphenationEnabled="True">
                    </FlowDocument>
            </FlowDocumentScrollViewer>
        </Border>

        <Button x:Name="CloseBtn" Grid.Row="2" Content="CLOSE" Click="Close_Click" 
                Width="100" Height="30" Margin="0,15,0,0"
                Background="#ff5500" Foreground="Black" FontWeight="Bold" Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="3">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>