<Window x:Class="ARC_Sight.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" 
        SizeToContent="WidthAndHeight" 
        WindowStyle="None" ResizeMode="NoResize" 
        Background="#1E1E1E" 
        WindowStartupLocation="CenterOwner" 
        BorderBrush="#ff5500" BorderThickness="1">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#333"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          Grid.Column="2" 
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" 
                                                Background="#333" 
                                                BorderBrush="#555" 
                                                BorderThickness="1" 
                                                CornerRadius="3">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" 
                                                      Grid.Column="1" 
                                                      Fill="White" 
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center" 
                                                      Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="Border" Property="Background" Value="#444"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" 
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,0,25,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>

                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="#1E1E1E" 
                                            BorderThickness="1"
                                            BorderBrush="#ff5500"
                                            CornerRadius="2">
                                        <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderThickness="0" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#3a3a3a"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#ff5500"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="Box" Width="16" Height="16" Background="#333" BorderBrush="#555" BorderThickness="1" CornerRadius="3" Margin="0,0,8,0">
                                <Path x:Name="CheckMark" Data="M 3,8 L 6,11 L 13,4" Stroke="#ff5500" StrokeThickness="2" Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Box" Property="BorderBrush" Value="#ff5500"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <StackPanel Margin="25" MinWidth="360" MaxWidth="450">
        <TextBlock x:Name="TitleBlock" Text="SETTINGS" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock x:Name="HotkeyLabel" Text="Hotkey" Foreground="#888" Margin="0,5"/>
        <TextBox x:Name="HotkeyBox" Text="F9" IsReadOnly="True" PreviewKeyDown="HotkeyBox_KeyDown" Cursor="Hand" FontWeight="Bold"/>

        <TextBlock x:Name="AlertLabel" Text="Alert (Minutes)" Foreground="#888" Margin="0,15,0,5"/>
        <TextBox x:Name="NotifyBox" Text="5"/>

        <CheckBox x:Name="SoundCheck" Content="Enable Alert Sound" Margin="0,15,0,0"/>
        <CheckBox x:Name="TimeCheck" Content="Show Local Time" Margin="0,10,0,0"/>

        <TextBlock x:Name="LangLabel" Text="Language" Foreground="#888" Margin="0,15,0,5"/>
        <ComboBox x:Name="LangCombo"/>

        <TextBlock x:Name="TwitchUserLabel" Text="Twitch Username (Contributors)" Foreground="#888" Margin="0,15,0,5"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TwitchUserBox" Grid.Column="0" VerticalAlignment="Center" Height="28"/>
            <Button x:Name="VerifyBtn" Content="VÉRIFIER" Click="VerifyTwitch_Click" Grid.Column="1" Width="80" Height="28" Margin="10,0,0,0" Background="#333" Foreground="White" FontSize="10" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Ellipse x:Name="TwitchAvatarCircle" Grid.Column="2" Width="28" Height="28" Margin="10,0,0,0" Visibility="Collapsed">
                <Ellipse.Fill>
                    <ImageBrush x:Name="TwitchAvatarBrush"/>
                </Ellipse.Fill>
            </Ellipse>
        </Grid>
        <TextBlock x:Name="TwitchHelpText" Foreground="#888" FontSize="11" FontStyle="Italic" TextWrapping="Wrap" Margin="0,5,0,0"/>

        <Border BorderBrush="#333" BorderThickness="0,1,0,0" Margin="0,20,0,15" Padding="0,15,0,0">
            <StackPanel>
                <TextBlock x:Name="AboutHeader" Text="À PROPOS" Foreground="White" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock x:Name="CreatedBy" Text="Created by..." Foreground="#aaa" FontSize="11" TextWrapping="Wrap"/>
                <TextBlock x:Name="CurrentVersion" Text="Version: ..." Foreground="#aaa" FontSize="11" Margin="0,2,0,0"/>
                <TextBlock x:Name="ApiSource" Text="Source API: ..." Foreground="#aaa" FontSize="11" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <Button x:Name="PatchNotesBtn" Content="View Patch Notes" Click="PatchNotes_Click" 
                HorizontalAlignment="Left" MinWidth="120" Height="25" 
                Background="Transparent" Foreground="#ff5500" BorderBrush="#ff5500" BorderThickness="1"
                Margin="0,0,0,15" Cursor="Hand" FontSize="11">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="3">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#333"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelBtn" Content="Cancel" Click="Cancel_Click" MinWidth="80" Width="Auto" Height="30" Padding="15,0" Background="#333" Foreground="White" Margin="0,0,10,0" Cursor="Hand"/>
            <Button x:Name="SaveBtn" Content="Save" Click="Save_Click" MinWidth="80" Width="Auto" Height="30" Padding="15,0" Background="#ff5500" Foreground="Black" FontWeight="Bold" Cursor="Hand"/>
        </StackPanel>
    </StackPanel>
</Window>